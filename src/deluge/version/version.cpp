// This is a stub to pull the generated code from the build directory in to the
// program, because CMake can't handle targets who have source files that don't
// exist at configure time.

#include "version.cpp.gen"
