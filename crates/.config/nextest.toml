# nextest CI profile: emit JUnit XML so the Rust test results feed the same
# publish-unit-test-result pipeline as the C++ CppUTest/CTest suites.
# `cargo nextest run --profile ci` writes target/nextest/ci/junit.xml.
[profile.ci.junit]
path = "junit.xml"
