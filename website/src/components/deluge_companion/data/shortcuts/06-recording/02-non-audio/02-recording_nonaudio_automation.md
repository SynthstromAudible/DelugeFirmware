capability: RECORDING
capability-order: 7
sub-capability: Recording (Non-Audio)
sub-capability-order: 3
sub-sub-capability: Recording (Automation)
sub-sub-capability-order: 2

# Record automation of parameter

#OFFICIAL #SYNTH #KIT

```shortcut
press(RECORD), press(PLAY), turn(PARAMETER)
```

Records automation for the chosen parameter while playback is running.

# Record MIDI automation

#OFFICIAL #MIDI

```shortcut
press(RECORD), press(PLAY), turn(PARAMETER)
```

CC with a '.' shown on the LCD indicates automation is already recorded.

# Record MIDI step automation

#OFFICIAL #MIDI

```shortcut
hold(GRID) + turn(PARAMETER)
```

Records MIDI automation per step.