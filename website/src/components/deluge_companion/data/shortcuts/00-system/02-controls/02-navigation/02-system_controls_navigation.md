capability: SYSTEM
capability-order: 1
sub-capability: System (Controls)
sub-capability-order: 2
sub-sub-capability: System (Navigation)
sub-sub-capability-order: 2

# Check current zoom level

#OFFICIAL #ARRANGER #SESSION_ROW #CLIP #AUTOMATION

```shortcut
press(X)
```

# Change current zoom level

#OFFICIAL #COMMUNITY #ARRANGER #SESSION_ROW #CLIP #WAVEFORM #AUTOMATION

```shortcut
hold(X) + turn(X)
```

When you `turn(X)` clockwise, the resolution doubles. So instead of 16th notes, you'll see 32nd notes displayed.

Similarly, when you `turn(X)` counterclockwise, the resolution is halved.

Note #1: White notes in song and clip mode require zooming in to see exact position in the timeline.

```community
The maximum zoom level has been increased. Now, the maximum zoom is the point the point where the entire timeline is represented by a single grid cell.
  - This allows for more flexibility when entering long notes and chord progressions.
  - While changing the zoom level, the horizontal encoder will briefly pause while passing the zoom level which represents the entire sequence. This is to prevent frustration from users who are used to the prior limitations.
```

# Scroll horizontally (left or right)

#OFFICIAL #ARRANGER #SESSION_ROW #CLIP #WAVEFORM #KEYBOARD #AUTOMATION

```shortcut
turn(X)
```

Scroll left and right across the grid.

# Scroll vertically (up or down)

#OFFICIAL #PERFORMANCE #ARRANGER #SESSION #CLIP_INSTRUMENT #KEYBOARD #AUTOMATION

```shortcut
turn(Y)
```

Scroll up and down across the main grid or sidebar.

# Navigate in Vertical Menu

#OFFICIAL #MENU

```shortcut
turn(SELECT)
```

Turn select encoder to scroll up and down in a vertical menu.

# Navigate on Current Horizontal Menu Page

#COMMUNITY #MENU #SYNTH #KIT #MIDI #CV

```shortcut
press(SYNTH), press(KIT), press(MIDI), press(CV)
```

With the current horizontal menu page open, press Synth, Kit, MIDI, or CV to select one of the four horizontal menu slots on the page.

# Navigate to the Previous or Next Horizontal Menu Page

#COMMUNITY #MENU #SYNTH #KIT #MIDI #CV

```shortcut
press(SCALE), press(CROSS)
```

Press scale to navigate to the previous horizontal menu page.
Press cross screen to navigate to the next horizontal menu page.

# Navigate to the Previous or Next Adjacent Horizontal Menu

#COMMUNITY #MENU #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + press(SCALE), hold(SHIFT) + press(CROSS)
```

Press shift + scale to navigate to the previous adjacent horizontal menu.
Press shift + cross screen to navigate to the next adjacent horizontal menu.

Moves to the next adjacent horizontal menu.

# Enter / Step into the currently selected menu

#OFFICIAL #MENU

```shortcut
press(SELECT)
```

Press select encoder to enter the currently selected menu.

# Navigate Backwards in Menu

#OFFICIAL #MENU

```shortcut
press(BACK)
```

Go back up one level from within the menu / browser / file system.

# Return to Session or Arranger from Menu

#COMMUNITY #MENU

```shortcut
press(SONG)
```

Exits a menu and returns to Session or Arranger View.

# Layered Shortcuts

#COMMUNITY #MENU #MENU_SOUND #SYNTH #KIT

```shortcut
hold(SHIFT) + press(GRID)
```

Cycles through layered shortcuts when a shortcut pad has multiple actions assigned.
