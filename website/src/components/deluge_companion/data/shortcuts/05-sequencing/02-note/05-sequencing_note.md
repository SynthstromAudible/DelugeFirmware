capability: SEQUENCING
capability-order: 6
sub-capability: Sequencing (Note)
sub-capability-order: 2

# Make long note

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
hold(GRID) + press(GRID_UNLIT)
```

Select two grid buttons on the same row.

# Make long note across the next screen

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
press(GRID), turn(X), hold(X) + press(GRID_UNLIT)
```

Move to the next screen, then select the two grid buttons on the same row.

# Adjust note velocity

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
hold(GRID) + turn(X)
```

Multiple notes can be selected. New notes default to the last velocity setting. Default velocity is 64, with a range of 0-127.

# Note probability; clockwise is iteration and counter is probability

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
hold(GRID) + turn(SELECT)
```

Turn counter-clockwise to adjust probability. Turn clockwise to adjust iteration.

# Note probability group

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
hold(GRID) + turn(SELECT)
```

When probability is the same, all notes follow the action of the first note in the sequence. A '.' signifies that a probability group exists.

# Copy notes

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
hold(LEARN) + press(X)
```

Copy applies to all notes in the vertical columns at the current zoom level, including notes not visible above and below the grid.

# Paste notes

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + hold(LEARN) + press(X)
```

Paste applies to all notes in the vertical columns at the current zoom level, including notes not visible above and below the grid.

# Cross screen editing, real time note cloning

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
press(CROSS)
```

Cross-screen mode is linked to zoom level. Turn it on or off if you want to apply changes at different zoom levels.

# Add instrument / row to kit

#OFFICIAL #KIT

```shortcut
hold(AUDITION) + press(KIT), turn(SELECT), press(SELECT)
```

Scroll to a sample. Press Back to go up one folder level.

# Shift all notes horizontally

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
hold(Y) + turn(X)
```

Shifts all clip notes horizontally left or right.

# Clear clip

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
hold(X) + press(BACK)
```

# Change clip color

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + turn(Y)
```

# Change row color - instrument row in kit

#OFFICIAL #KIT

```shortcut
hold(SHIFT) + hold(AUDITION) + turn(Y)
```

# Reorder kit clip row up or down

#OFFICIAL #KIT

```shortcut
hold(AUDITION) + hold(LAUNCH) + turn(Y)
```

# Note nudge individual note

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
hold(GRID_LIT) + hold(X) + turn(X)
```

# Clip play direction

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + press(GRID)
```

The play-direction pad may not be labelled on the grid.

# Euclidean - adjust selected note row events / notes

#OFFICIAL #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(AUDITION) + hold(Y) + turn(Y)
```

Adds or removes Euclidean events / notes in the selected row.

# Euclidean - adjust selected note row length

#OFFICIAL #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(AUDITION) + turn(X)
```

Changes the length of the currently selected note row.

# Euclidean - rotate selected note row events or notes

#OFFICIAL #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(AUDITION) + press(X) + turn(X)
```

Rotates the selected note row horizontally.

# Quantize / humanize

#OFFICIAL #SYNTH #KIT #MIDI #CV

```shortcut
hold(AUDITION) + turn(TEMPO)
```

Turn clockwise to quantize or counter-clockwise to humanize. Applies to one row or all rows depending on context.

# Enter Velocity View

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(AUDITION) + press(VELOCITY_PATCH_SOURCE)
```

Opens the note velocity editor from Instrument Clip View or Automation View.

# Select Note Row in Velocity Editor

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
press(AUDITION)
```

Selects the current note row in the velocity editor.

# Select Note Row in Velocity Editor with Vertical Encoder

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
turn(Y)
```

Alternative way to select a different note row in the velocity editor.

# Note Row Probability

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(AUDITION) + turn(SELECT)
```

Adjusts the selected note row's probability in the velocity editor.

# Note Row Iterance

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(AUDITION) + turn(SELECT)
```

Adjusts the selected note row's iterance in the velocity editor.

# Note Row Velocity

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(GRID) + turn(X)
```

Adjusts the selected note's velocity in the velocity editor.

# Note Row Fill

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(AUDITION) + press(SYNC)
```

Adjusts the selected note row's fill setting when the Community Feature is enabled.

# Note Repeat

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(GRID) + press(Y)
```

Adjusts note repeat for the selected note in the velocity editor.

# Enter Note Editor

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(GRID) + press(SELECT)
```

Opens the note editor menu for the held note.

# Exit Note Editor

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
press(BACK)
```

Exits the note editor menu.

# Note Editor Cross-Screen Editing

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
press(CROSS)
```

Edits notes across multiple screens while in the note editor.

# Enter Note Row Editor

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(AUDITION) + press(SELECT)
```

Opens the note row editor menu for the held note row.

# Exit Note Row Editor

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
press(BACK)
```

Exits the note row editor menu.

# Note Row Transpose

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
press(Y) + turn(Y)
```

Transposes the selected note row while editing velocity data.

# Clear Selected Note Row

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(X) + press(BACK)
```

Clears the notes in the currently selected note row.

# Rotate Note Row

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(Y) + turn(X)
```

Rotates the selected note row horizontally in velocity editor.

# Quantize or Humanize Notes

#COMMUNITY #SYNTH #KIT #MIDI #CV #AUTOMATION #AUTOMATION_VELOCITY

```shortcut
hold(AUDITION) + press(TEMPO) + turn(TEMPO)
```

Adjusts note quantize or humanize amount in the velocity editor.

# Transpose screen

#COMMUNITY #CLIP #SYNTH #MIDI #CV

```shortcut
hold(X) + turn(Y)
```

# Transpose screen in octave

#COMMUNITY #CLIP #SYNTH #MIDI #CV

```shortcut
hold(X) + press(Y) + turn(Y)
```

# Save Melodic Pattern

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
press(SAVE) + press(X)
```

Saves the current melodic pattern to a pattern file.

# Load Melodic Pattern

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
press(LOAD) + press(X)
```

Loads a melodic pattern from a pattern file.

# Load Melodic Pattern without Overwriting Notes

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
press(SAVE) + press(CROSS) + press(X)
```

Loads a pattern into the clipboard without replacing the existing notes.

# Load Melodic Pattern without Scaling

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
press(LOAD) + press(SCALE) + press(X)
```

Loads a pattern without scaling the original file.

# Paste Loaded Pattern

#COMMUNITY #SYNTH #MIDI #CV #KIT

```shortcut
hold(SHIFT) + press(LEARN) + press(X)
```

Pastes a loaded pattern from the clipboard.

# Gentle Paste Loaded Pattern

#COMMUNITY #SYNTH #MIDI #CV #KIT

```shortcut
hold(SHIFT) + press(CROSS) + press(X)
```

Gentle-pastes a loaded pattern from the clipboard.

# Note Probability

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + press(NOTE_PATCH_SOURCE)
```

# Randomizer Lock

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + press(RANDOM_PATCH_SOURCE)
```

# Velocity Probability

#COMMUNITY #SYNTH #KIT #MIDI #CV

```shortcut
hold(SHIFT) + press(VELOCITY_PATCH_SOURCE)
```

# Transpose clip by octave

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
hold(Y) + turn(Y)
```

# Transpose current clip by semitone (scale off)

#COMMUNITY #SYNTH #MIDI #CV

```shortcut
hold(SHIFT) + hold(Y) + turn(Y)
```

# Transpose all clips by semitone (scale on)

#OFFICIAL #COMMUNITY #SYNTH #MIDI #CV

```shortcut
hold(SHIFT) + hold(Y) + turn(Y)
```
